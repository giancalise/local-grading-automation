# SolidGrade Desktop — pinned runtime dependencies (SPEC_v0.2 §2.2, §2.4)
# Target interpreter: Python 3.12 (not 3.14 — see §2.3; PyInstaller hooks
# are more mature on 3.12 and nothing here needs 3.13+ syntax).
#
# scipy is UNDECLARED by any import statement in this codebase but is
# mandatory at runtime: trimesh.VoxelGrid.fill() calls
# scipy.ndimage.binary_fill_holes. Do not remove it because a dependency
# scan says it's unused — see DISCOVERY_REPORT.md Phase 4.2 and the §2.4
# self-test, which exists specifically to catch this class of failure.

blinker==1.9.0
click==8.5.0
Flask==3.1.3
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
numpy==2.5.2
pywin32==312
scipy==1.18.1
trimesh==5.0.0
Werkzeug==3.1.8
